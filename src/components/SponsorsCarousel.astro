---
import { Image } from "astro:assets";

interface Sponsor {
  name: string;
  logo: ImageMetadata;
}

interface Props {
  sponsors: Sponsor[];
  title?: string;
  subtitle?: string;
}

const { 
  sponsors = [],
  title = "Nuestros Colaboradores",
  subtitle = "Empresas Increíbles"
} = Astro.props;

// Duplicamos el array para el efecto de scroll infinito
const allSponsors = [...sponsors, ...sponsors];
---

<section class="py-20 bg-white overflow-hidden">
  <div class="container mx-auto px-4 mb-12">
    <div class="max-w-2xl">
      {subtitle && <h2 class="text-primary font-bold tracking-widest uppercase text-sm mb-2">{subtitle}</h2>}
      {title && <h3 class="text-4xl md:text-5xl font-extrabold text-dark leading-tight">{title}</h3>}
      <div class="w-16 h-1 bg-primary mt-4"></div>
    </div>
  </div>

  <div class="relative flex overflow-x-hidden group max-w-[1480px] mx-auto">
    <div class="animate-scroll flex gap-8 py-4 whitespace-nowrap min-w-full">
      {allSponsors.map((sponsor) => (
        <div class="flex shrink-0 w-64 h-32 items-center justify-center p-4 bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md hover:scale-120 transition-all duration-300">
          <Image 
            src={sponsor.logo} 
            alt={`Logo de ${sponsor.name}`} 
            class="w-full max-w-full max-h-full object-contain object-center"
            loading="lazy"
          />
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .animate-scroll {
    display: flex;
    animation: scroll 10s linear infinite; 
  }

  .group:hover .animate-scroll {
    animation-play-state: paused;
  }

  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-100%);
    }
  }
</style>
